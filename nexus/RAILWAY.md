# 🚀 Деплой NEXUS на Railway - Пошаговое руководство

## Содержание

1. [Подготовка](#1-подготовка)
2. [Регистрация на Railway](#2-регистрация-на-railway)
3. [Создание проекта](#3-создание-проекта)
4. [Добавление PostgreSQL](#4-добавление-postgresql)
5. [Добавление Redis](#5-добавление-redis)
6. [Подключение GitHub репозитория](#6-подключение-github-репозитория)
7. [Настройка переменных окружения](#7-настройка-переменных-окружения)
8. [Первый деплой](#8-первый-деплой)
9. [Настройка домена](#9-настройка-домена)
10. [Добавление Workers (фоновые задачи)](#10-добавление-workers)
11. [Мониторинг и логи](#11-мониторинг-и-логи)
12. [Решение проблем](#12-решение-проблем)

---

## 1. Подготовка

### 1.1 Убедитесь, что у вас есть:

- [ ] GitHub аккаунт с репозиторием проекта
- [ ] Anthropic API ключ (для AI генерации контента)
- [ ] Опционально: ключи социальных сетей (Twitter, Telegram, и т.д.)
- [ ] Опционально: ключи для генерации изображений (OpenAI, Stability AI)

### 1.2 Проверьте файлы проекта

В репозитории должны быть:

```
nexus/
├── Dockerfile          ← Для сборки контейнера
├── railway.toml        ← Конфигурация Railway
├── package.json        ← Зависимости Node.js
├── prisma/
│   └── schema.prisma   ← Схема базы данных
└── src/                ← Исходный код
```

### 1.3 Запушьте код в GitHub

```bash
cd /path/to/Marketing-Project
git add .
git commit -m "Prepare for Railway deployment"
git push origin main
```

---

## 2. Регистрация на Railway

### Шаг 2.1: Откройте сайт Railway

1. Перейдите в браузере на **https://railway.app**
2. Нажмите кнопку **"Login"** в правом верхнем углу

### Шаг 2.2: Войдите через GitHub

1. Нажмите **"Login with GitHub"**
2. Авторизуйте Railway для доступа к вашему GitHub
3. Разрешите доступ к репозиториям (можно выбрать конкретные)

### Шаг 2.3: Выберите план

| План | Стоимость | Особенности |
|------|-----------|-------------|
| **Trial** | Бесплатно | $5 кредитов, для тестирования |
| **Hobby** | $5/месяц | Для личных проектов |
| **Pro** | $20/месяц | Для команд |

> 💡 **Рекомендация**: Начните с Trial для тестирования, затем перейдите на Hobby.

---

## 3. Создание проекта

### Шаг 3.1: Создайте новый проект

1. На дашборде Railway нажмите **"+ New Project"**
2. Выберите **"Empty Project"** (пустой проект)

### Шаг 3.2: Назовите проект

1. Нажмите на название проекта вверху (по умолчанию случайное)
2. Введите понятное имя, например: **"nexus-marketing"**
3. Нажмите Enter для сохранения

> 📸 Вы увидите пустой холст проекта — сюда мы добавим сервисы.

---

## 4. Добавление PostgreSQL

### Шаг 4.1: Добавьте базу данных

1. Нажмите **"+ New"** в правой части экрана
2. Выберите **"Database"**
3. Выберите **"Add PostgreSQL"**

### Шаг 4.2: Дождитесь создания

1. Railway автоматически создаст PostgreSQL сервер
2. Это занимает 10-30 секунд
3. Статус изменится на **"Available"** (зелёный)

### Шаг 4.3: Проверьте переменные

1. Нажмите на карточку PostgreSQL
2. Перейдите во вкладку **"Variables"**
3. Вы увидите автоматически созданные переменные:

```
DATABASE_URL=postgresql://postgres:xxx@xxx.railway.internal:5432/railway
PGHOST=xxx.railway.internal
PGPORT=5432
PGUSER=postgres
PGPASSWORD=xxx
PGDATABASE=railway
```

> ✅ `DATABASE_URL` будет использоваться приложением автоматически.

---

## 5. Добавление Redis

### Шаг 5.1: Добавьте Redis

1. Нажмите **"+ New"** на холсте проекта
2. Выберите **"Database"**
3. Выберите **"Add Redis"**

### Шаг 5.2: Дождитесь создания

1. Railway создаст Redis сервер
2. Статус станет **"Available"**

### Шаг 5.3: Проверьте переменные

1. Нажмите на карточку Redis
2. Вкладка **"Variables"**:

```
REDIS_URL=redis://default:xxx@xxx.railway.internal:6379
REDISHOST=xxx.railway.internal
REDISPORT=6379
REDISUSER=default
REDISPASSWORD=xxx
```

> ✅ `REDIS_URL` будет использоваться для очередей и кэша.

---

## 6. Подключение GitHub репозитория

### Шаг 6.1: Добавьте сервис из GitHub

1. Нажмите **"+ New"** на холсте
2. Выберите **"GitHub Repo"**
3. Найдите ваш репозиторий в списке
4. Нажмите на него

> 🔒 Если репозитория нет в списке:
> - Нажмите **"Configure GitHub App"**
> - Добавьте доступ к нужному репозиторию
> - Вернитесь и обновите список

### Шаг 6.2: Выберите папку (если нужно)

Если ваш проект в подпапке `nexus/`:

1. Нажмите на созданный сервис
2. Перейдите в **"Settings"**
3. Найдите **"Root Directory"**
4. Введите: `nexus`
5. Нажмите **"Save"**

### Шаг 6.3: Railway обнаружит конфигурацию

Railway автоматически найдёт:
- `railway.toml` — настройки деплоя
- `Dockerfile` — инструкции сборки

> ⏳ Первый деплой начнётся автоматически, но пока остановите его — нужно настроить переменные!

### Шаг 6.4: Остановите деплой (если начался)

1. Нажмите на сервис приложения
2. Вкладка **"Deployments"**
3. Нажмите **"Cancel"** на текущем деплое

---

## 7. Настройка переменных окружения

### Шаг 7.1: Откройте настройки переменных

1. Нажмите на сервис приложения (GitHub repo)
2. Перейдите во вкладку **"Variables"**

### Шаг 7.2: Добавьте ссылки на БД

Нажмите **"+ New Variable"** и добавьте:

#### Подключение к PostgreSQL:
```
Имя: DATABASE_URL
Значение: ${{Postgres.DATABASE_URL}}
```

> 💡 `${{Postgres.DATABASE_URL}}` — это ссылка на переменную из сервиса PostgreSQL. Railway автоматически подставит реальное значение.

#### Подключение к Redis:
```
Имя: REDIS_URL
Значение: ${{Redis.REDIS_URL}}
```

### Шаг 7.3: Добавьте API ключи

Добавьте каждую переменную по одной:

#### Обязательные:

| Имя переменной | Значение | Описание |
|----------------|----------|----------|
| `ANTHROPIC_API_KEY` | `sk-ant-api03-...` | Ключ Anthropic Claude |
| `NODE_ENV` | `production` | Режим продакшена |
| `NEXT_PUBLIC_APP_URL` | `https://your-app.railway.app` | URL приложения (обновите после п.9) |

#### Для генерации изображений (опционально):

| Имя переменной | Значение | Описание |
|----------------|----------|----------|
| `OPENAI_API_KEY` | `sk-...` | Для DALL-E 3 |
| `STABILITY_API_KEY` | `sk-...` | Для Stability AI |
| `REPLICATE_API_TOKEN` | `r8_...` | Для Replicate/Flux |

#### Для социальных сетей (добавьте нужные):

**Twitter/X:**
```
TWITTER_API_KEY=...
TWITTER_API_SECRET=...
TWITTER_ACCESS_TOKEN=...
TWITTER_ACCESS_SECRET=...
```

**Telegram:**
```
TELEGRAM_BOT_TOKEN=...
```

**Facebook:**
```
FACEBOOK_ACCESS_TOKEN=...
FACEBOOK_PAGE_ID=...
```

**Discord:**
```
DISCORD_BOT_TOKEN=...
DISCORD_CHANNEL_ID=...
```

**VK:**
```
VK_ACCESS_TOKEN=...
VK_GROUP_ID=...
```

### Шаг 7.4: Проверьте все переменные

После добавления у вас должно быть минимум:

```
DATABASE_URL      = ${{Postgres.DATABASE_URL}}
REDIS_URL         = ${{Redis.REDIS_URL}}
ANTHROPIC_API_KEY = sk-ant-...
NODE_ENV          = production
NEXT_PUBLIC_APP_URL = https://...
```

> ✅ Готово к деплою!

---

## 8. Первый деплой

### Шаг 8.1: Запустите деплой

1. Нажмите на сервис приложения
2. Вкладка **"Deployments"**
3. Нажмите **"Deploy"** или Railway начнёт автоматически

### Шаг 8.2: Наблюдайте за сборкой

Откройте логи сборки — вы увидите этапы:

```
Step 1/15: FROM node:20-alpine AS deps
Step 2/15: RUN apk add --no-cache libc6-compat openssl
...
Step 10/15: RUN npx prisma generate
Step 11/15: RUN npm run build
...
Step 15/15: CMD ["sh", "-c", "npx prisma migrate deploy && node server.js"]
```

> ⏱ Первая сборка занимает 3-5 минут.

### Шаг 8.3: Проверьте запуск

После сборки в логах будет:

```
Running prisma migrate deploy...
Applying migrations...
Migration applied successfully.

▲ Next.js 14.2.18
- Local:  http://localhost:3000
- Ready in 1.2s
```

### Шаг 8.4: Получите временный URL

1. Railway автоматически создаст URL вида:
   `https://nexus-marketing-production.up.railway.app`
2. Нажмите на него для проверки

### Шаг 8.5: Проверьте Health Check

Откройте в браузере:
```
https://your-app.railway.app/api/health
```

Ожидаемый ответ:
```json
{
  "status": "healthy",
  "timestamp": "2024-01-15T10:30:00.000Z",
  "version": "0.1.0",
  "services": {
    "database": "connected",
    "redis": "connected"
  }
}
```

> ✅ Если видите "healthy" — приложение работает!

---

## 9. Настройка домена

### Вариант A: Использовать домен Railway

1. Нажмите на сервис приложения
2. **Settings** → **Networking** → **Public Networking**
3. Нажмите **"Generate Domain"**
4. Получите URL: `https://your-app.railway.app`

### Вариант B: Свой домен

1. **Settings** → **Networking** → **Custom Domain**
2. Введите ваш домен: `nexus.yourdomain.com`
3. Добавьте CNAME запись у регистратора:
   ```
   Тип: CNAME
   Имя: nexus
   Значение: your-app.railway.app
   ```
4. Дождитесь проверки (до 24 часов, обычно 5-10 минут)

### Шаг 9.1: Обновите NEXT_PUBLIC_APP_URL

После получения постоянного домена:

1. **Variables** в сервисе приложения
2. Измените `NEXT_PUBLIC_APP_URL`:
   ```
   NEXT_PUBLIC_APP_URL=https://nexus.yourdomain.com
   ```
3. Railway автоматически передеплоит приложение

---

## 10. Добавление Workers

Для фоновых задач (публикация по расписанию, обработка вебхуков) нужен отдельный сервис.

### Шаг 10.1: Добавьте второй сервис из GitHub

1. **"+ New"** → **"GitHub Repo"**
2. Выберите тот же репозиторий
3. Назовите сервис: **"nexus-workers"**

### Шаг 10.2: Настройте Workers

1. Откройте сервис **"nexus-workers"**
2. **Settings** → **Root Directory**: `nexus`
3. **Settings** → **Build** → **Custom Build Command**:
   ```
   npm install && npx prisma generate
   ```
4. **Settings** → **Deploy** → **Custom Start Command**:
   ```
   npm run workers
   ```

### Шаг 10.3: Добавьте переменные

Скопируйте переменные из основного сервиса:

```
DATABASE_URL=${{Postgres.DATABASE_URL}}
REDIS_URL=${{Redis.REDIS_URL}}
ANTHROPIC_API_KEY=...
NODE_ENV=production
```

### Шаг 10.4: Запустите Workers

1. Сохраните настройки
2. Workers начнёт автоматически

> ✅ Теперь у вас 2 сервиса: Web App и Workers

---

## 11. Мониторинг и логи

### 11.1: Просмотр логов

1. Нажмите на любой сервис
2. Вкладка **"Logs"**
3. Логи обновляются в реальном времени

### 11.2: Фильтрация логов

- Ищите по ключевым словам
- Фильтруйте по времени
- Скачивайте для анализа

### 11.3: Метрики

1. Вкладка **"Metrics"** в сервисе
2. Смотрите:
   - CPU использование
   - RAM использование
   - Сетевой трафик

### 11.4: Health Check мониторинг

Railway автоматически проверяет `/api/health`:
- Каждые 30 секунд
- При ошибке — перезапускает сервис
- 3 неудачные попытки → редеплой

---

## 12. Решение проблем

### Проблема: Деплой падает

**Симптом**: Build failed

**Решение**:
1. Проверьте логи сборки
2. Частые причины:
   - Ошибка в `package.json`
   - Отсутствует зависимость
   - Ошибка TypeScript

```bash
# Проверьте локально:
cd nexus
npm install
npm run build
```

### Проблема: Не подключается к БД

**Симптом**: "Connection refused" или "ECONNREFUSED"

**Решение**:
1. Проверьте `DATABASE_URL` — должен быть `${{Postgres.DATABASE_URL}}`
2. Убедитесь, что PostgreSQL сервис запущен (зелёный)
3. Попробуйте **Redeploy**

### Проблема: Миграции не применяются

**Симптом**: "Table does not exist"

**Решение**:
```bash
# Через Railway CLI:
railway run npx prisma migrate deploy
```

Или добавьте в Dockerfile:
```dockerfile
CMD ["sh", "-c", "npx prisma migrate deploy && node server.js"]
```

### Проблема: Workers не запускаются

**Симптом**: Workers сервис падает сразу

**Решение**:
1. Проверьте Start Command: `npm run workers`
2. Убедитесь, что в `package.json` есть скрипт `workers`
3. Проверьте логи Workers сервиса

### Проблема: Приложение медленное

**Симптом**: Долгая загрузка страниц

**Решение**:
1. Проверьте метрики CPU/RAM
2. Возможно, нужно больше ресурсов:
   - **Settings** → **Resources** → Увеличьте RAM
3. Оптимизируйте запросы к БД

---

## Итоговая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     Railway Project                          │
│                    "nexus-marketing"                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────┐     ┌────────────────┐                  │
│  │   PostgreSQL   │     │     Redis      │                  │
│  │   (Database)   │     │    (Cache)     │                  │
│  │                │     │                │                  │
│  │ DATABASE_URL   │     │  REDIS_URL     │                  │
│  └───────┬────────┘     └───────┬────────┘                  │
│          │                      │                            │
│          └──────────┬───────────┘                            │
│                     │                                        │
│          ┌──────────┴──────────┐                            │
│          │                     │                            │
│  ┌───────▼───────┐    ┌───────▼───────┐                    │
│  │  NEXUS App    │    │ NEXUS Workers │                    │
│  │  (Next.js)    │    │   (BullMQ)    │                    │
│  │               │    │               │                    │
│  │ Port: 3000    │    │ Background    │                    │
│  │ Health: /api/ │    │ Jobs          │                    │
│  │        health │    │               │                    │
│  └───────┬───────┘    └───────────────┘                    │
│          │                                                  │
│  ┌───────▼───────┐                                         │
│  │ Custom Domain │                                         │
│  │ nexus.your    │                                         │
│  │ domain.com    │                                         │
│  └───────────────┘                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Стоимость

| Компонент | Примерная стоимость |
|-----------|---------------------|
| PostgreSQL | ~$5-7/месяц |
| Redis | ~$3-5/месяц |
| Web App | ~$5-10/месяц |
| Workers | ~$3-5/месяц |
| **Итого** | **~$16-27/месяц** |

> 💡 Hobby план ($5/месяц) включает $5 кредитов — может покрыть часть расходов.

---

## Чеклист перед продакшеном

- [ ] PostgreSQL создан и работает
- [ ] Redis создан и работает
- [ ] Все переменные окружения настроены
- [ ] Health check возвращает "healthy"
- [ ] Домен настроен и работает
- [ ] Workers запущены (если нужны)
- [ ] SSL сертификат активен (автоматически)
- [ ] Логи не показывают ошибок

---

## Полезные команды Railway CLI

```bash
# Установка CLI
npm install -g @railway/cli

# Логин
railway login

# Подключение к проекту
railway link

# Запуск команды в контексте Railway
railway run npm run db:migrate

# Просмотр логов
railway logs

# Открыть в браузере
railway open
```

---

**🎉 Поздравляем! Ваш NEXUS теперь работает на Railway!**
